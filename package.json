{
  "name": "prism",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "android": "npx cap sync android && cd android && .\\gradlew.bat assembleDebug",
    "android:deploy": "node scripts/android-deploy.mjs",
    "xbox:sync": "vite build && node scripts/xbox-sync.mjs",
    "xbox:build": "npm run xbox:sync && node scripts/xbox-build.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "hls.js": "^1.6.16",
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
