# Loaded automatically by Vite/Vitest for every test run (mode "test") regardless of
# whether a real .env is also present on this machine - see tmdb.test.js and .gitignore's
# comment on this file. Not a real TMDB key; only makes VITE_TMDB_API_KEY deterministically
# truthy so tests exercise tmdb.js's real fetch path against a mocked global.fetch.
VITE_TMDB_API_KEY=test-fixture-key
